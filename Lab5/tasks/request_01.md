# Order Validation

Orders with quantity less than or equal to zero are currently accepted.

Update the implementation so invalid quantities are rejected.

Requirements:

- quantity must be greater than zero
- preserve the existing API
- do not modify unrelated functionality